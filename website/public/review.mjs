const frame=document.querySelector('iframe');if(frame){const apply=name=>{frame.classList.toggle('mobile',name==='mobile');for(const id of ['desktop','mobile'])document.getElementById(id).setAttribute('aria-pressed',String(id===name));history.replaceState(null,'','?view='+name);};for(const name of ['desktop','mobile'])document.getElementById(name).onclick=()=>apply(name);apply(new URLSearchParams(location.search).get('view')==='mobile'?'mobile':'desktop');}
